# Vega Wallet UI E2E tests

To run the UI automation tests, run:

```bash
yarn nx run wallet-mock:e2e
```

To open Cypress and run in interactive mode, run:

```bash
yarn nx run wallet-mock:e2e --watch
```
